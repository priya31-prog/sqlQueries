Triangle order (20-1)

DELIMITER //

CREATE PROCEDURE PrintOrder()
BEGIN
    DECLARE points INT DEFAULT 20;
    WHILE points > 0 DO
        SELECT REPEAT('* ',points);
        SET points = points -1;
    END WHILE;
END //

DELIMITER ;
